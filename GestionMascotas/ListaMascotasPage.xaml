<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionMascotas.ListaMascotasPage"
             Title="Mis Mascotas">
    <Grid RowDefinitions="*, Auto" Padding="10">
        <ListView x:Name="lstMascotas" SeparatorVisibility="Default">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid ColumnDefinitions="*, Auto" Padding="10">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Nombre}" FontAttributes="Bold" FontSize="18"/>
                                <Label Text="{Binding Especie}" TextColor="Gray"/>
                            </VerticalStackLayout>
                            <Button Grid.Column="1" Text="Eliminar" 
                                    BackgroundColor="Red" 
                                    CommandParameter="{Binding Id}"
                                    Clicked="OnEliminarClicked" 
                                    HeightRequest="40"/>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="1" Text="Actualizar Lista" Clicked="CargarMascotas" Margin="0,10,0,0"/>
    </Grid>
</ContentPage>